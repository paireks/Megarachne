using System;
using System.Collections.Generic;
using Rhino.Geometry;
using MegarachneEngine;
using Xunit;


namespace MegarachneEngineTests
{
    /*public class TestCheckIfPointsAreSameHelper : TheoryData<Point3d, Point3d, double, bool>
    {
        public TestCheckIfPointsAreSameHelper()
        {
            Add(new Point3d(0, 0, 0), new Point3d(0, 0, 0), 0.0001, true);
        }
    }
    public class TestTools
    {
        [Theory]
        [ClassData(typeof(TestCheckIfPointsAreSameHelper))]
        public void TestCheckIfPointsAreSame(Point3d pointA, Point3d pointB, double tolerance, bool areSame)
        {
            bool arePointsTheSame = Tools.CheckIfPointsAreSame(pointA, pointB, tolerance);
            Assert.Equal(areSame, arePointsTheSame);
        }
    }*/
}
